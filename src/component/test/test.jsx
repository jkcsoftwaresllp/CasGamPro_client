import { HeaderMain } from "../../layoutDash/jsx/HeaderMain";
export const Test = () => {
  return (
    <div>
      <button onClick={() => console.log("Test button clicked")}>
        Test Button
      </button>
    </div>
  );
};
