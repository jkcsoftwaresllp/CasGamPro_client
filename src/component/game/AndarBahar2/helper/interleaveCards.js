import { interleaveCards as sendableCard } from "../../teenPattiT20/helper/interleaveCards";

// Helper function to interleave cards alternately between playerA and playerB
export const interleaveCards = (cards) => sendableCard(cards);
